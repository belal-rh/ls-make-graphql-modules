id: LS-COURSE-001
name: Alle Kurse abrufen
object: Course
action: fetch
status: verified
last_verified_at: 2026-07-20

operation:
  name: AuthoredCourses
  sha256Hash: a70e28d2b8370d93ce039c75abe29c0ed646259d799d1522bf31e0c22b813c0b

endpoint:
  method: POST
  path: /graphql

authentication:
  type: bearer
  token_request: ../../../docs/authentication.md

parameters: {}

request:
  operationName: AuthoredCourses
  variables: {}
  extensions:
    persistedQuery:
      version: 1
      sha256Hash: a70e28d2b8370d93ce039c75abe29c0ed646259d799d1522bf31e0c22b813c0b

response:
  success_path: data.authoredCourses
  cardinality: many
  outputs:
    id:
      path: data.authoredCourses[].id
      type: NodeID
    sid:
      path: data.authoredCourses[].sid
      type: SID
    name:
      path: data.authoredCourses[].name
      type: string
    isPublished:
      path: data.authoredCourses[].isPublished
      type: boolean

errors:
  graphQl_errors_path: errors
  persisted_query_not_found:
    action: Capture a new RAW request and update sha256Hash.

make_module:
  name: listCourses
  label: Kurse auflisten
  type: search
